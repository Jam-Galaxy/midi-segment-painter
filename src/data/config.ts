import { Transforms } from "@/VisualMidiSegment/hooks/useSong";

export const DEBOUNCE_DELAY = 1000; //ms
export const DEFAULT_WIDTH = 200; //px
export const DEFAULT_HEIGHT = 100;

export const DEFAULT_TRANSFORMS: Transforms = {
  pixelsPerTick: 0.1,
  segmentHeight: 100,
}